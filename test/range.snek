(fun (range low high)
  (block
    (print low)
    (print high)
    (let (n high)
      (let (lst nil)
        (loop
          (if (= n low) (break lst)
            (block
              (set! lst (pair n lst))
              (set! n (+ n -1)))))))))

(range 0 10)
